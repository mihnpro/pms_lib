# PMS Library - Property Management System

C++ библиотека для управления гостиничным бизнесом

## Структура проекта

- `src/` - Исходный код
- `include/` - Заголовочные файлы
- `config/` - Файлы конфигурации
  
## Сборка

```bash
mkdir build && cd build
cmake ..
make
